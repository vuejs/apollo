{
  "name": "vue-apollo-monorepo",
  "version": "3.0.0",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs"
  },
  "devDependencies": {
    "vuepress": "^0.14.2"
  }
}